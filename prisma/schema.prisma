generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Organization {
  id           String        @id @default(cuid())
  name         String        @unique
  users        User[]
  roadmaps     Roadmap[]
  backlogItems BacklogItem[]
  createdAt    DateTime      @default(now())
}

model User {
  id             String       @id @default(cuid())
  email          String       @unique
  name           String
  role           String       @default("viewer")
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])
  createdAt      DateTime     @default(now())
}

model Roadmap {
  id                 String            @id @default(cuid())
  name               String
  organizationId     String
  organization       Organization      @relation(fields: [organizationId], references: [id])
  startDate          String
  endDate            String
  programIncrements  ProgramIncrement[]
  epics              Epic[]
  features           Feature[]
  milestones         Milestone[]
  savedAt            DateTime?
  createdAt          DateTime          @default(now())
}

model Milestone {
  id        String   @id @default(cuid())
  roadmapId String
  roadmap   Roadmap  @relation(fields: [roadmapId], references: [id])
  name      String
  date      String
  status    String   @default("Planned")
  type      String   @default("Release")
  featureId String?
  feature   Feature? @relation(fields: [featureId], references: [id])
  createdAt DateTime @default(now())
}

model ProgramIncrement {
  id        String   @id @default(cuid())
  roadmapId String
  roadmap   Roadmap  @relation(fields: [roadmapId], references: [id])
  name      String
  startDate String
  endDate   String
  backlogItems BacklogItem[]
  createdAt DateTime @default(now())
}

model Epic {
  id          String   @id @default(cuid())
  roadmapId   String
  roadmap     Roadmap  @relation(fields: [roadmapId], references: [id])
  title       String
  description String?
  acceptanceCriteria String?
  notes       String?
  status      String   @default("planned")
  startDate   String?
  endDate     String?
  rank        Int      @default(0)
  features    Feature[]
  backlogItems BacklogItem[]
  createdAt   DateTime @default(now())
}

model Feature {
  id          String   @id @default(cuid())
  roadmapId   String
  roadmap     Roadmap  @relation(fields: [roadmapId], references: [id])
  epicId      String?
  epic        Epic?    @relation(fields: [epicId], references: [id])
  title       String
  description String?
  acceptanceCriteria String?
  notes       String?
  status      String   @default("planned")
  startDate   String?
  endDate     String?
  rank        Int      @default(0)
  pi          String?
  team        String?
  milestones  Milestone[]
  tasks       Task[]
  createdAt   DateTime @default(now())
}

model Task {
  id          String   @id @default(cuid())
  featureId   String
  feature     Feature  @relation(fields: [featureId], references: [id], onDelete: Cascade)
  title       String
  description String?
  status      String   @default("Planned")
  startDate   String
  endDate     String
  createdAt   DateTime @default(now())
}

model BacklogItem {
  id             String       @id @default(cuid())
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id])
  title          String
  description    String?
  type           String       @default("feature")
  status         String       @default("new")
  priority       Int          @default(0)
  epicId         String?
  epic           Epic?        @relation(fields: [epicId], references: [id])
  programIncrementId String?
  programIncrement ProgramIncrement? @relation(fields: [programIncrementId], references: [id])
  createdAt      DateTime     @default(now())
}

model ExternalLink {
  id          String   @id @default(cuid())
  itemType    String
  itemId      String
  system      String
  externalId  String
  externalUrl String
  createdAt   DateTime @default(now())
}
